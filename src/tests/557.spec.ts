import reverseWords from '../557';

test("557", () => {
    expect(reverseWords("Let's take LeetCode contest")).toBe("s'teL ekat edoCteeL tsetnoc")
    expect(reverseWords("God Ding")).toBe("doG gniD")
})
